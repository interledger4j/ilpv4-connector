/**
 * Contains Integration tests that cover any functionality related to Ilp-over-Http (aka, Blast). If adding a new test
 * to this package, be sure to annotate it with `@Category(IlpOverHttp.class)` so it gets picked up by the CI build
 * system.
 */
package org.interledger.connector.it.blast;
