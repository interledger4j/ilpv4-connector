/**
 * Contains Integration tests that cover any functionality related to Open Payments. If adding a new test to this
 * package, be sure to annotate it with `@Category(OpenPayments.class)` so it gets picked up by the CI build system.
 */
package org.interledger.connector.it.opa;
