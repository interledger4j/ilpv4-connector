/**
 * Contains Integration tests that cover any functionality related to cluster coordination. If
 * adding a new test to this package, be sure to annotate it with `@Category(Coordination.class)` so it gets picked up
 * by the CI build system.
 *
 */
package org.interledger.connector.it.coordination;
