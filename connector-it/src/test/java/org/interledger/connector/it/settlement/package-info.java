/**
 * Contains Integration tests that cover any functionality related to Settlement Engines as defined in IL-RFC-TBD. If
 * adding a new test to this package, be sure to annotate it with `@Category(Settlement.class)` so it gets picked up by
 * the CI build system.
 *
 * @see "https://github.com/interledger/rfcs/pull/536"
 */
package org.interledger.connector.it.settlement;
